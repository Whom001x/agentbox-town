# 代码审查与重构计划

更新时间：2026-06-14

## 当前结构判断

项目已经从前端演算迁移到 Node 后台，但主文件 `ai-town-v2-server.js` 仍承担过多职责：

- HTTP 路由与静态文件服务
- AI 配置、Key 分流、重试与调用日志
- 存档读写、拆文件存档、修复与归一化
- Node 小镇运行循环
- Agent 提示词、payload 构造、结果解析
- 建镇流程、角色/地点/社会结构生成
- 移动、行动、状态结算、事件传播的部分运行时逻辑

这会导致三个问题：

- 任意小改都容易影响整个服务启动。
- 相同概念在多个地方重复，例如 JSON 读写、死亡冻结、需求结算、地点人员校验。
- 后续要做后台进程、手机端、PC 端同时接入时，边界不清。

## 已完成的第一轮重构

新增 `ai-town-json-utils.js`：

- `sanitizeForJson`
- `safeJsonClone`
- `readJsonIfExists`
- `writeJsonFile`

`ai-town-v2-server.js` 里保留原函数名作为薄包装，外部行为不变。

这样做的目的：

- 统一 JSON 清洗和 UTF-8 写入。
- 后续存档模块、配置模块、运行进度模块可以复用。
- 降低坏 JSON、循环引用、非法 Unicode 造成存档损坏的概率。

## 建议拆分顺序

### 1. 存档模块

目标文件：

- `ai-town-save-store.js`

迁移内容：

- `safeSaveName`
- `savePathFor`
- `saveFolderFor`
- `assertInsideSaveDir`
- `writeFolderSave`
- `readSplitSavePayload`
- `readSavePayload`
- `writeRuntimePayload`
- `listSaves`

收益：

- 存档结构独立。
- 后续“一个存档一个文件夹”的设计更容易维护。
- 修复存档不会误碰 AI 调用和路由。

### 2. 运行进度模块

目标文件：

- `ai-town-runtime-progress.js`

迁移内容：

- `runtimeProgress`
- `persistRuntimeProgress`
- `loadRuntimeProgress`
- `resetRuntimeProgress`
- `beginRuntimeProgress`
- `updateRuntimeProgress`
- `completeRuntimeProgress`
- `failRuntimeProgress`

收益：

- 前端/手机端能稳定读取后台运行状态。
- 后续可以把进度队列持久化为每个存档自己的 `runtime/progress.json`。

### 3. Agent 提示词模块

目标文件：

- `ai-town-prompts.js`

迁移内容：

- `systemPrompt`
- `userPrompt`
- 各 Agent schema 描述

收益：

- 提示词更容易审查越权。
- 可以给每个 Agent 单独版本号。
- 方便做提示词测试。

### 4. Node 运行时模块

目标文件：

- `ai-town-runtime.js`
- `ai-town-runtime-agents.js`
- `ai-town-runtime-settlement.js`

迁移内容：

- Scheduler / PreJudgement
- AgentAction
- TimePassage
- StateSettlement
- PostAgents
- DailyAgents
- WorldGuard 调用

收益：

- 每回合流程更清晰。
- 可以更容易做并发批处理。
- 死亡、医疗、职业服务这类规则能集中测试。

### 5. 建镇模块

目标文件：

- `ai-town-setup.js`

迁移内容：

- 地点生成
- 角色生成
- 家庭/邻里/同事/同学关系
- AI 补全与本地兜底

收益：

- 新建存档逻辑独立。
- 可单独测试 30/100/200 人建镇质量。

## 最高风险点

- `ai-town-node-core.js` 和部分存档里已有中文乱码/坏 JSON 历史痕迹，不能用 PowerShell `Set-Content` 大范围重写。
- `ai-town-v2.html` 仍然保留大量旧前端运行逻辑，后续要逐步瘦身，但不能一次删除。
- 死亡/健康系统已经迁到 Node 后，必须优先保证死亡角色不参与行动，濒危计时不能复用不同原因。
- AI retry 现在和 Agent 批处理耦合较深，后续要按 Agent 单位排队重试，而不是阻塞整个循环。

## 重构原则

- 每次只拆一个职责边界。
- 先抽纯函数和无状态工具，再抽有状态模块。
- 保留原函数名做兼容层，等验证稳定后再删除旧包装。
- 每次改完至少运行：
  - `node --check ai-town-v2-server.js`
  - `node --check ai-town-node-core.js`
  - `node --check` 新增模块
- 同步到运行目录前，必须确认没有把 `saves/`、`android/`、构建缓存同步过去。

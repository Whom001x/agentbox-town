# AgentBox Town 真实性改进建议

> 基于当前源码审查，本文只提出修改建议，不直接修改代码。重点改善小镇模拟中“需求、生理、情绪、死亡、医疗、日常行为”的真实感。

---

## 1. 总体判断

当前项目已经具备真实小镇模拟的基本结构：角色有位置、需求、情绪、睡眠、记忆、关系、医疗状态、行动过程和死亡状态。后端也已经把时间推进、生理衰减、基础进食、医疗升级、移动到达和死亡判定集中在 `ai-town-node-core.js`。

但当前更像“结构真实”，还不是“动态真实”。主要问题是：

- 需求变化偏线性，缺少低谷加速恶化。
- 情绪变化偏阈值触发，缺少连续渐进。
- 死亡判定对不同风险区分不足。
- 进食和医疗偏兜底，不像完整生活流程。
- 个体差异不足，同年龄/职业的角色差别不明显。
- 地点更像数值恢复区，还不像有工作人员、排队、库存和服务能力的真实机构。

---

## 2. 最高优先级：需求衰减改为非线性

### 当前问题

`timeDecayChanges` 使用固定线性速率扣减需求。例如成年人饥饿每小时扣 2.5，健康每小时扣 0.18，安全每小时扣 0.18。

这会导致 hunger 80 到 78 和 hunger 10 到 8 的体感差异不明显。真实生活中，一个人越接近极限，状态恶化越快。

### 建议方案

新增通用倍率函数：

```js
function decayMultiplier(currentValue, strength = 1.5) {
  const value = Math.max(0, Math.min(100, Number(currentValue) || 0));
  const missing = (100 - value) / 100;
  return 1 + missing * strength;
}
```

应用到关键需求：

```js
hunger: -hours * baseHungerRate * decayMultiplier(agent.needs.hunger, 1.2),
health: -hours * baseHealthRate * decayMultiplier(agent.needs.health, 2.0),
safety: -hours * baseSafetyRate * decayMultiplier(agent.needs.safety, 2.5)
```

### 预期效果

| 需求值 | 衰减倍率 | 表现 |
|---|---:|---|
| 90 | 轻微加速 | 状态稳定 |
| 60 | 中等加速 | 开始有压力 |
| 30 | 明显加速 | 进入危险区 |
| 10 | 快速恶化 | 接近崩溃 |

建议先只对 `hunger`、`health`、`safety` 启用非线性，避免角色突然大面积死亡。

---

## 3. 高优先级：死亡判定按原因区分

### 当前问题

当前死亡逻辑主要围绕 `health <= 0` 且持续 1440 分钟。这个设计对慢性病比较温和，但对事故类风险不真实。

现实中不同风险的致命速度不同：

- 饥饿是慢性风险。
- 健康归零可能是急病或慢性恶化。
- safety 归零更像事故、火灾、暴力、严重外伤，应更快致命。
- stress 归零可能是极端心理或心血管风险，也不应和饥饿一样。

### 建议方案

给不同原因设置不同死亡窗口：

| 条件 | 建议窗口 | 说明 |
|---|---:|---|
| `safety <= 0` | 30–240 分钟 | 事故、火灾、暴力、严重外伤 |
| `health <= 0` | 720–1440 分钟 | 急病或慢性病恶化 |
| `hunger <= 0` | 2880–4320 分钟 | 长期饥饿 |
| `stress <= 0` | 720–1440 分钟 | 极端压力或心源性风险 |

推荐逻辑：

```js
if (safetyZeroMinutes >= 180 && undiscoveredMinutes >= 30) {
  // 事故死亡
}

if (healthZeroMinutes >= 1440 && !hasRescue) {
  // 健康归零且未救治
}

if (hungerZeroMinutes >= 2880 && undiscoveredMinutes >= 720) {
  // 长期饥饿
}

if (stressZeroMinutes >= 720 && undiscoveredMinutes >= 180) {
  // 极端压力风险
}
```

### 预期效果

慢性问题不会过早杀死角色，但严重事故会产生真实后果，小镇也会更依赖邻居发现、家属反应、送医救助等社会链条。

---

## 4. 高优先级：情绪从阈值触发改为渐进变化

### 当前问题

当前情绪联动类似：

```js
if (agent.needs.hunger < 22) adjustEmotion(agent, { tired: 1, angry: 0.6, calm: -0.8 });
```

这意味着 hunger 21 和 hunger 0 的情绪影响接近。真实情况应是越饿越疲惫、越烦躁、越不平静。

### 建议方案

新增压力强度函数：

```js
function needPressure(value, threshold) {
  const safeValue = Math.max(0, Math.min(100, Number(value) || 0));
  if (safeValue >= threshold) return 0;
  return (threshold - safeValue) / threshold;
}
```

应用示例：

```js
const hungerPressure = needPressure(agent.needs.hunger, 60);
adjustEmotion(agent, {
  tired: hungerPressure * 3,
  angry: hungerPressure * 1.5,
  calm: -hungerPressure * 2
});
```

推荐映射：

| 需求 | 情绪影响 |
|---|---|
| hunger 低 | tired 上升，angry 上升，calm 下降 |
| stress 低 | anxious 上升，calm 下降，hopeful 下降 |
| social 低 | lonely 上升，sad 上升 |
| health 低 | tired 上升，hopeful 下降，anxious 上升 |
| safety 低 | anxious 大幅上升，calm 大幅下降 |

---

## 5. 高优先级：基础进食从整点兜底改为意图驱动

### 当前问题

`applyBasicLifeMaintenance` 主要在 7 点、12 点、18 点触发进食。如果角色错过整点，或者不在有食物的地点，就依赖紧急兜底。

这更像系统补数值，不像真实生活。

### 建议方案

把进食拆成三层：

1. **计划性进食**：早餐、午餐、晚餐时段产生吃饭意图。
2. **机会性进食**：路过早餐店、商店、餐馆时，如果 hunger 偏低，自动考虑买食物。
3. **紧急进食**：hunger 极低时才由系统兜底补食。

建议将整点判断改为时间段判断：

```js
function isMealWindow(hour) {
  return (hour >= 7 && hour <= 9)
    || (hour >= 11 && hour <= 13)
    || (hour >= 17 && hour <= 19);
}
```

新增意图判断：

```js
function shouldSeekFood(agent, world) {
  const hunger = Number(agent.needs?.hunger ?? 70);
  const hour = Math.floor((Number(world.clock || 0) % 1440) / 60);
  return (isMealWindow(hour) && hunger < 55) || hunger < 30;
}
```

### 预期效果

角色行为会更像：

```text
感觉饿了 -> 想吃饭 -> 前往早餐店/餐馆/家 -> 等待/购买/进食 -> hunger 恢复
```

而不是后台直接给 hunger 加 45。

---

## 6. 中优先级：增加个体差异

### 当前问题

当前角色主要通过年龄和职业影响衰减。现实中体质、病史、抗压能力、恢复速度差异很大。

### 建议方案

在 `ensureAgentShape` 中补充：

```js
agent.physiology ||= {
  metabolism: 1.0,
  stressResistance: 1.0,
  immuneStrength: 1.0,
  recoverySpeed: 1.0
};

agent.chronicConditions ||= [];
```

推荐慢性条件：

- `hypertension`：压力低时更容易拖累健康。
- `diabetes`：饥饿与健康联动更强。
- `asthma`：天气、安全、健康影响更强。
- `anxiety_disorder`：stress 下降更快，anxious 更敏感。
- `frail`：健康恢复慢，死亡阈值更低。

### 应用位置

- `timeDecayChanges`：影响基础衰减。
- `applyMedicalEscalation`：影响医疗优先级。
- `evaluateMortalityV2`：影响死亡阈值。
- 多维需求联动：影响雪崩速度。

---

## 7. 中优先级：医疗系统改成救助链

### 当前问题

当前系统已有 `medicalState`、`knownBy`、`undiscoveredMinutes`，方向很好。但医疗仍偏向“到诊所有医护就恢复一部分数值”。

### 建议方案

将医疗过程拆成阶段：

```text
不适出现 -> 自己发现/他人发现 -> 求助 -> 陪同 -> 去诊所 -> 候诊 -> 处理 -> 观察 -> 恢复/回家
```

推荐 `activeProcess.stage`：

- `not_yet_discovered`
- `self_notice`
- `nearby_notice`
- `ask_help`
- `escort_to_clinic`
- `waiting_triage`
- `basic_treatment`
- `observation`
- `recovery`

### 预期效果

医疗不再只是数值补丁，而会成为小镇社会关系的一部分：邻居发现、家属陪同、医生处理、后续探望都能生成事件和记忆。

---

## 8. 中优先级：安全事件引入突变机制

### 当前问题

`safety` 目前主要是缓慢变化。但真实世界中的安全风险往往来自突发事件。

### 建议方案

新增事件型需求变化：

```js
function applyEventImpactToNeeds(agent, event) {
  if (event.type === "minor_accident") return { safety: -15, health: -8, stress: -12 };
  if (event.type === "major_accident") return { safety: -45, health: -30, stress: -35 };
  if (event.type === "conflict") return { safety: -20, stress: -25, social: -10 };
  if (event.type === "helped") return { safety: 15, social: 10, stress: 8 };
  return {};
}
```

推荐事件类型：

- `minor_accident`：轻微摔倒、碰撞。
- `major_accident`：车祸、火灾、严重摔倒。
- `conflict`：争吵、威胁、冲突。
- `crime`：盗窃、袭击。
- `rescue`：被帮助、被送医。
- `good_news`：好消息、表彰、关系改善。

---

## 9. 中优先级：地点从“回血格子”改为服务机构

### 当前问题

地点对需求的影响目前比较直接，例如在公寓恢复 hygiene，在广场/市场恢复 social。这合理但粗糙。

### 建议方案

给地点增加运行状态：

```js
place.runtime = {
  open: true,
  staffCount: 2,
  queueLength: 5,
  serviceSpeed: 1.0,
  supplies: {
    food: 30,
    medicine: 10
  }
};
```

地点服务应检查：

- 是否开放。
- 是否有工作人员。
- 是否排队过长。
- 是否有库存。
- 角色是否符合服务条件。

### 预期效果

餐馆、诊所、学校、商店不再只是“站上去恢复数值”，而是有真实运行状态的机构。

---

## 10. 中优先级：睡眠质量影响次日状态

### 当前问题

当前有睡眠窗口和 `sleepQuality`，但睡眠对第二天状态的影响还可以更明显。

### 建议方案

在起床或 0 点结算：

```js
if (agent.sleepQuality < 40) {
  adjustNeeds(agent, { health: -5, stress: -8, comfort: -5 });
  adjustEmotion(agent, { tired: 10, anxious: 4, calm: -5 });
}

if (agent.sleepQuality > 80) {
  adjustNeeds(agent, { health: 3, stress: 5, comfort: 8 });
  adjustEmotion(agent, { calm: 6, hopeful: 4, tired: -8 });
}
```

### 预期效果

失眠、熬夜、夜间事故会影响第二天行为，让角色日常节奏更连续。

---

## 11. 中优先级：记忆影响行为选择

### 当前问题

角色有记忆结构，但行为选择主要仍由当前状态和 AI 判断决定。现实中记忆会影响下一次选择。

### 建议方案

在 Scheduler 和 AgentAction 上下文中加入记忆权重：

- 最近在某地点受伤：降低再次前往该地点的意愿。
- 某人曾帮助自己：更容易向其求助。
- 某地点经常提供食物：饥饿时更容易想到去那里。
- 某人发生冲突：再次相遇时增加 safety/stress 压力。
- 家人/好友生病：增加探望和陪同概率。

### 预期效果

角色会形成长期个性和历史惯性，而不是每轮只看当前数值。

---

## 12. 工程建议：增加数值回归测试

### 当前问题

项目目前没有测试。修改生理和死亡系统后，容易出现两类极端问题：角色大面积死亡，或者角色永远不会死亡。

### 建议方案

新增 `scripts/check-life-model.js`，用简单 Node 脚本验证关键场景：

1. 健康成年人 24 小时不吃饭不会死亡。
2. hunger 连续 72 小时为 0 会进入死亡风险。
3. safety 为 0 且无人发现，应在数小时内死亡。
4. 在诊所有医护时，health 低的角色能被救治。
5. 睡眠期间不会继续普通行动。
6. 死亡角色不会再移动或行动。

### 预期效果

每次调整数值后，都可以快速确认模型没有崩坏。

---

## 13. 推荐实施顺序

### 第一阶段：低风险数值改造

1. 添加 `decayMultiplier`，只对 hunger、health、safety 启用。
2. 情绪从阈值触发改为渐进变化。
3. 死亡阈值区分 safety、health、hunger、stress。
4. 增加 `scripts/check-life-model.js` 验证核心场景。

### 第二阶段：行为真实感改造

1. 基础进食改为“意图 + 地点 + 兜底”。
2. 医疗流程拆成多个阶段。
3. 引入事件型需求突变。
4. 睡眠质量影响次日状态。

### 第三阶段：长期模拟增强

1. 个体差异：体质、病史、抗压能力、恢复速度。
2. 地点服务能力：开放、工作人员、排队、库存。
3. 记忆影响行为选择。
4. 家庭、邻居、工作关系参与救助和传播。

---

## 14. 最小可行修改清单

如果只想用最少改动提高真实感，建议先做这 5 项：

- `timeDecayChanges` 增加非线性倍率。
- `evaluateMortalityV2` 区分 `safety`、`health`、`hunger`、`stress` 的死亡窗口。
- `applyTimeDecay` 的情绪影响改为渐进式。
- `applyBasicLifeMaintenance` 不再只依赖 7/12/18 整点，改为 7-9、11-13、17-19 时间段。
- 新增 `scripts/check-life-model.js`，验证核心生理模型不会过快或过慢。

---

## 15. 结论

当前系统的真实感基础已经存在，但主要是“结构真实”，不是“动态真实”。

优先改造数值曲线和死亡原因，会立刻提升真实感；再改进食、医疗、事件突变和记忆影响，会让角色行为更像真实生活中的人。
